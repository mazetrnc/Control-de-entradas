# Configuración para enviar el reporte quincenal por correo automáticamente.
# Completa los 4 valores de abajo (quita el # de cada línea que uses).
# Mientras falte alguno, el correo simplemente no se envía (sin errores).
#
# Ejemplo con una cuenta de Gmail (necesitas crear una 'contraseña de
# aplicación' en la configuración de seguridad de esa cuenta de Google;
# NO uses la contraseña normal de la cuenta):
#
# servidor=smtp.gmail.com
# puerto=465
# remitente=tu_correo@gmail.com
# contraseña_app=xxxx xxxx xxxx xxxx